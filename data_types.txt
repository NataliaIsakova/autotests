Неизменяемые типы данных не могут быть изменены после их создания.
Это значит, что каждый раз, когда кажется, что вы меняете неизменяемый объект,
на самом деле создается новый объект, а переменная теперь ссылается на него.